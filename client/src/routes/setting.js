import React from 'react'
import Tap from '../partials/Tap'

const Setting = () => {
    return (
        <div className='display-flex flex-col'>
            <Tap></Tap>
            <h1 className='text-center'> 셋팅입니다.</h1>
        </div>
    );
};

export default Setting